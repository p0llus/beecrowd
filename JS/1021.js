var N = parseFloat(require('fs').readFileSync('/My Drive/CODE/beecrowd/JS/stdin', 'utf8'));
console.log("NOTAS:");
let valor = parseInt(N / 100);
console.log(`${valor} nota(s) de R$ 100.00`);
N = N % 100;
valor = parseInt(N / 50);
console.log(`${valor} nota(s) de R$ 50.00`);
N = N % 50;
valor = parseInt(N / 20);
console.log(`${valor} nota(s) de R$ 20.00`);
N = N % 20;
valor = parseInt(N / 10);
console.log(`${valor} nota(s) de R$ 10.00`);
N = N % 10;
valor = parseInt(N / 5);
console.log(`${valor} nota(s) de R$ 5.00`);
N = N % 5;
valor = parseInt(N / 2);
console.log(`${valor} nota(s) de R$ 2.00`);
N = N % 2;
console.log("MOEDAS:");
valor = parseInt(N / 1);
console.log(`${Math.floor(valor)} moeda(s) de R$ 1.00`);
N = N % 1;
valor = (N / 0.50);
console.log(`${Math.floor(valor)} moeda(s) de R$ 0.50`);
N = N % 0.50;
valor = (N / 0.25);
console.log(`${Math.floor(valor)} moeda(s) de R$ 0.25`);
N = N % 0.25;
valor = (N / 0.10);
console.log(`${Math.floor(valor)} moeda(s) de R$ 0.10`);
N = N % 0.10;
valor = (N / 0.05);
console.log(`${Math.floor(valor)} moeda(s) de R$ 0.05`);
N = N % 0.05;
valor = (N / 0.01 + 0.00001); // "+ 0.00001 por causa do bug no beecrowd"
console.log(`${Math.floor(valor)} moeda(s) de R$ 0.01`);